import {LANGUAGE_KEY, LANGUAGE_NAME} from '../../../constants/constant';

export const userDetails = {
  appLanguage: {
    languageKey: LANGUAGE_KEY.ENGLISH,
    title: LANGUAGE_NAME.ENGLISH,
  },
};
